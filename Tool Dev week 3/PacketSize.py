#ISS 212 Scripting WK 3 Assignment
#Resources Pythin Scenario 1 Example Script

#Prompt the user to input the packet size in bytes and store them as an integer
packet_size = int(input("Enter the packet size in bytes: "))

#Print True if the packet size is bigger than or equal to 2600, Say False otherwise
print(packet_size >= 2600)
